### Tips for using git:

think "A + C + P"
add + commit + push

options for push include 'origin master'

git pull
git status
git add (filename)
git commit -m "message"
git push origin master




[<<< Back](README.md)
